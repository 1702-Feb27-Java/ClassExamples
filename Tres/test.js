    function printShape(shape, height, character)
    {
        switch(shape)
        {
            case "Triangle":
                for (var i =0; i < height; i ++)
                {
                    for(var x = 0; x <= i; x++)
                        console.log(character);
                }
                break;
            case "Square":
                for (var i = 0; i < height; i++)
                {
                    
                }
                break;
            case "Diamond":

                break;
    }
