package com.homework1.question18;

public abstract class AbstractSuperClass {
	
	//abstract class to check for upper case characters in a string
	abstract boolean upperCaseChars(String input);
	
	//convert all lower case characters to to upper case in the string
	abstract String lowerCaseConvert(String input);
	
	//convert the input string to an integer and add 10, print the result to console
	abstract void integerConvert(String input);
	
	
}
